name: pull requests

on:
  pull_request_target:
    types: [opened]

permissions:
  pull-requests: write

jobs:
  uneditable:
    uses: its9xquoc/shipyard-cli/.github/workflows/pull-requests.yml@main
